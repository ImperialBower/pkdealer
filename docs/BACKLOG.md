# Backlog

> Generated by the `/backlog` skill (2026-06-20) from EPIC docs, code markers, and
> the changelog. An index, not the source of truth — each item links to its doc.
> Tech-debt detail lives in [`docs/TECHNICAL_DEBT.md`](TECHNICAL_DEBT.md).

## EPICs / Features — outstanding

### EPIC-41 — Reproducible Scenarios · **Not started** (largest greenfield)
`docs/EPIC-41_Reproducible_Scenarios.md`
- `pkcore` deck-injection primitive (`set_next_deck` + seeded deck gen).
- Proto: `ScenarioConfig` / `ScenarioMode` / `ConfigureScenario` RPC.
- Service: scenario engine (in-process bots, deck injection, stack reset).
- Tournament mode; duplicate mode (reset stacks each hand); seed-generated decks.

### EPIC-43 — PokerBench Integration · **Phase 1 done, Phase 2 not started** (cross-repo)
`docs/EPIC-43_PokerBench_Integration.md`, `docs/EPIC-43_pkcore_PokerBench_spec.md`
- ✅ Phase 1 — pkcore `pokerbench` module (scenario model, loaders, `score_action`) shipped in pkcore (module present).
- ◻️ Phase 2 — pkdealer offline eval harness `crates/pkdealer_pokerbench` (**not yet created**).
- ◻️ Phase 3 — gated; Phases 4–5 — stretch.

### EPIC-44 — Token Accounting & Cost Simulation · **Phases 0–3 done; backlog open**
`docs/EPIC-44_Token_Accounting_and_Cost_Simulation.md` ("Backlog — more realistic pricing")
- **B1 (highest value)** — real networked LLM backends: `pkdealer_agent_openai`, `pkdealer_agent_gemini` (mirror the working Claude backend; exact usage + true cost native, removes tokenizer skew). Needs API keys.
- **B2** — Claude-exact via Anthropic `count_tokens` (network+key); per-notional-model tokenizer selection instead of the single `o200k_base` proxy.
- **B3** — Phase 4 pricing realism: cached-input modeling, batch (50%) discount, reasoning-token multiplier.
- **B4** — DRY the duplicated `parse_price_as` into `pkdealer_pricing` (see tech debt).

### External render (separate repos)
- EPIC-44 token + cost columns: the data (`SeatInfo.input_tokens/output_tokens/cost_micro_usd`) ships from this repo; the **rendered columns live in `pkspectator` / pktui** (external).

## Tech debt
See [`docs/TECHNICAL_DEBT.md`](TECHNICAL_DEBT.md) — no inline `TODO`/`FIXME`
markers in source; a one-time automated review seeded ~15 machine-proposed items
(🤖) for triage. Highest-value: scope the file-wide `cast_possible_truncation` allow
and fix the unchecked `as` casts; bound the recorder eviction (O(n²)); enum-ify
`HandState::street`.

## Recently completed (context — not outstanding)
- ✅ EPIC-20 Autonomous Game Loop · ✅ EPIC-21 Spectator (moved to `pkspectator` repo)
- ✅ EPIC-22 OTel · ✅ EPIC-23 Bot Agents (all `pkdealer_agent_*` crates exist; doc status stale)
- ✅ EPIC-24 Demo · ✅ EPIC-25 Arena Recorder & Export · ✅ EPIC-40 Local LLM Backend
- ✅ EPIC-42 Dynamic Arena Runner · ✅ EPIC-44 Phases 0–3 (costsim, live token/cost columns, `pkdealer_pricing`, `--exact`)
