# pricing.toml — notional per-million-token rates (USD), input / output.
#
# Consumed by `pkdealer_costsim` (EPIC-44 Phase 0) to simulate what a recorded
# arena session would have cost on a commercial API. Local Ollama play has no
# real dollar cost; these rates are a *notional* overlay. Keys match the model
# ids recorded in `AgentFidelity.model` (the same id space as `arena.toml`).
#
# Rates drift — re-verify against provider pricing pages. Verified June 2026.

[models."gpt-5.5"]
input = 5.00
output = 30.00

[models."gpt-4.1"]
input = 2.00
output = 8.00

[models."gpt-4.1-mini"]
input = 0.40
output = 1.60

[models."gpt-4.1-nano"]
input = 0.10
output = 0.40

[models."claude-opus-4-8"]
input = 5.00
output = 25.00

[models."claude-sonnet-4-6"]
input = 3.00
output = 15.00

[models."claude-haiku-4-5"]
input = 1.00
output = 5.00

[models."gemini-3.1-pro"]
input = 2.00
output = 12.00

[models."deepseek-v3.2"]
input = 0.14
output = 0.28
